# Rust
/target/

# Nix / devenv / direnv
.devenv/
.devenv.flake.nix
devenv.local.nix
.direnv/
result
result-*

# Node / portal
node_modules/

# Local SQLite offline buffer (agent dev runs)
*.db
*.db-journal
*.db-wal
*.db-shm
