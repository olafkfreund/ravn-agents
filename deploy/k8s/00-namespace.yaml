# Namespace for the in-cluster Ravn agents (#59). The control plane itself
# runs outside the constrained cluster by default (design D2).
apiVersion: v1
kind: Namespace
metadata:
  name: ravn-system
  labels:
    app.kubernetes.io/part-of: ravn
